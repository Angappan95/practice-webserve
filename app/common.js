const addition = (a, b) => a + b;
const subraction = (a, b) => a - b;
const mul = (a, b) => a * b;
const division = (a, b) => a / b;

export { addition, subraction, mul as multiplication, division };
